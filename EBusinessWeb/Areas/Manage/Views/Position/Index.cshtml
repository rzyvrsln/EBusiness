@using EBusinessEntity.Entities;

@model ICollection<Position>
<main id="main" class="main">

    <div class="pagetitle">
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a asp-area="Manage" asp-controller="Home" asp-action="Index">Home</a></li>
                <li class="breadcrumb-item">Positions</li>
                <li class="breadcrumb-item active">See</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12 container">

                <div class="card container">
                    <div class="card-body">
                        <h5 class="card-title">Positions</h5>
                        <!-- Active Table -->
                        <table class="table table-borderless">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Position</th>
                                    <th scope="col">Created Date</th>
                                    <th scope="col">Updated Date</th>
                                    <th scope="col"></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var positions in Model ?? new List<Position>())
                                {
                                    <tr>
                                        <th scope="row">@positions.Id</th>
                                        <td>@positions.Name</td>
                                        <td>@positions.CreateAt.Day/@positions.CreateAt.Month/@positions.CreateAt.Year</td>
                                        <td>@positions.UpdateAt</td>
                                        <td><a asp-area="Manage" asp-controller="Position" asp-action="Remove" asp-route-Id="@positions.Id" class="btn btn-danger" title="Remove"><i class="bi bi-trash"></i></a></td>
                                        <td><a asp-area="Manage" asp-controller="Position" asp-action="Edit" asp-route-Id="@positions.Id" class="btn btn-warning" title="Edit"><i class="bx bxs-edit"></i></a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <!-- End Tables without borders -->

                    </div>
                </div>


            </div>


        </div>
    </section>

</main><!-- End #main -->
<!-- ======= Footer ======= -->